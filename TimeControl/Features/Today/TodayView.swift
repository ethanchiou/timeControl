import SwiftUI
import TimeControlCore

/// Placeholder; replaced in a later milestone.
struct TodayView: View {
    var body: some View {
        ContentUnavailableView("Today", systemImage: "sun.max", description: Text("Coming soon"))
            .navigationTitle("Today")
    }
}
