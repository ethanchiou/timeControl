import SwiftUI
import TimeControlCore

/// Placeholder; replaced in a later milestone.
struct WeekView: View {
    var body: some View {
        ContentUnavailableView("Week", systemImage: "calendar", description: Text("Coming soon"))
            .navigationTitle("Week")
    }
}
