import SwiftUI
import TimeControlCore

/// Placeholder; replaced in a later milestone.
struct TodosView: View {
    var body: some View {
        ContentUnavailableView("Todos", systemImage: "checklist", description: Text("Coming soon"))
            .navigationTitle("Todos")
    }
}
